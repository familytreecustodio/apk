name: Build APK
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Buildozer Action
        uses: Ente-AI/Buildozer-Action@v1.2.1
        with:
          buildozer_version: stable
